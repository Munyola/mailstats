```xml
<Manifest>
  <AgentType>Mac_Cocoa</AgentType>
  <Resources />
  <TimeoutSeconds>0</TimeoutSeconds>
</Manifest>
```
```csharp
Func<string,string> ParseString = (email) => {
	if (email.IndexOf ('\"') < 0)
		return email;
	return email.Split ('\"') [1];
}

ParseString("\"James Clancey\" <clancey@clancey.usa>");
ParseString("nat@nat.org");

```

